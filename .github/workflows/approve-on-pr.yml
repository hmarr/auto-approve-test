name: Approve on pull_request

on:
  pull_request:
  issue_comment:

jobs:
  auto-approve:
    runs-on: ubuntu-latest
    steps:
    - uses: dolfinus/auto-approve-action@v2
      with:
        pull-request-number: 3
        github-token: 
